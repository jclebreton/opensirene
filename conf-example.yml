logger:
  level: debug
  format: text

server:
  host: 127.0.0.1
  port: 8080
  debug: false
  cors:
    enabled: false
    permissive_mode: true
  prefix:
    api: "/api/v1"
    admin: "/admin"

database:
  user: sir
  password: sirensiren
  name: opensirenedb
  host: database
  port: 5432
  sslmode: disable

prometheus:
  prefix: opensirene

crontab:
  download_path: "downloads"
  every_x_hours: 3
